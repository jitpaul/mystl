#ifndef FORWARDLIST_H
#define FORWARDLIST_H
#endif

namespace mystd
{
template <class T>
class forward_list
{
    forward_list();
    find(T);
    void push_back(T);
    void push_front(T;
    void pop_front(T);
    void pop_back(T);

    size_t size();
};
}; // namespace mystd